# MultyPy
A educational app for practicing the multiplication table
# MultiPy - Multiplication Practice

A terminal-based multiplication table practice tool built with Python and Textual.

## Features

- **Practice Modes**:
  - **Simple Mode**: Choose the correct answer from 3 options.
  - **Normal Mode**: Type the answer directly.
- **Customizable Settings**:
  - Set multiplication table range (e.g., 2 to 12).
  - Set number of questions per session.
  - Set time limit per question.
- **Detailed Statistics**:
  - Track accuracy and speed (Correct Per Minute).
  - View summary after each session.

## Installation

1.  Clone the repository.
2.  Install dependencies:
    ```bash
    pip install -r requirements.txt
    ```

## Usage

Run the application:
```bash
python run.py
```

## Building

To build a standalone executable:
```powershell
.\build.ps1
```
